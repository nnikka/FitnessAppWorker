{
  "name": "fitness-app-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that proxies AI meal parsing for the Fitness App.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260710.1",
    "typescript": "^5.5.0",
    "wrangler": "^4.111.0"
  }
}
